# User-local configs

Scratch space for personal, machine-specific, or one-off training configs. Everything here except
this file is gitignored.

Nothing in this directory may be referenced by tracked docs, tests, or tooling. A config worth
citing in the repo is a config worth committing: put it under `configs/` proper and let the
reference in `configs/config_reference.yaml` cover its keys.
